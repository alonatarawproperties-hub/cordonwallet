REPLIT TASK (ONE-PROMPT, COPY/PASTE): Add TrustWallet-like “Contract & Security” info in Solana Asset About tab (SPL + Token-2022) — minimal changes, don’t touch swap/send/approvals/portfolio.

Scope rules (IMPORTANT):
- ONLY edit: client/screens/AssetDetailScreen.tsx (and only add required imports there).
- DO NOT modify swap, send, firewall, route, portfolio hooks, wallet context, navigation, or any other file.
- No refactors. No moving code. Keep risk low.

Goal:
Inside AssetDetailScreen About tab (tabs are inline: holdings/history/about), add a new section card “Contract & Security” that shows on-chain mint safety details like Trust Wallet:
1) Token program: SPL or Token-2022 (or Unknown)
2) Mintable: YES/NO (mintAuthority present?)
3) Freezable: YES/NO (freezeAuthority present?)
Special-case SOL (native): show “Native SOL” and skip mint checks.

Data source:
- Must read the mint account on-chain via existing Solana connection already used in this screen (do NOT create a new Connection per render).
- Use @solana/spl-token unpackMint with correct program ID determined from mint account owner (TOKEN_PROGRAM_ID vs TOKEN_2022_PROGRAM_ID).

UI rules:
- Must match Cordon theme: use theme.backgroundDefault, theme.border, theme.textSecondary, theme.accent, theme.success, theme.danger.
- Use the same card styling as the existing Stats card (statsCard + statRow). Add small “YES/NO” pills.
- Risk color rule: Mintable YES = danger (warning), NO = success. Freezable YES = danger, NO = success.

Performance rules:
- Fetch only when activeTab === "about" and chain is Solana and address is a mint (not SOL).
- Cache per mint in-memory Map for the session to avoid repeated RPC calls.
- Clear state on mint/address change to prevent stale display.
- Add loading state (“Fetching on-chain security…”) and graceful error state (“Unable to load security details”).

Implementation steps (do exactly):

1) In AssetDetailScreen.tsx imports, add (avoid duplicates):
import { PublicKey } from "@solana/web3.js";
import { TOKEN_PROGRAM_ID, TOKEN_2022_PROGRAM_ID, unpackMint } from "@solana/spl-token";

2) Near other local types at top of file, add:
type TokenSecurity = {
  tokenProgram: "SPL" | "Token-2022" | "Unknown";
  mintable: boolean;
  freezable: boolean;
};
const securityCache = new Map<string, TokenSecurity>(); // key = mint address

3) Inside the component, add state:
const [tokenSecurity, setTokenSecurity] = useState<TokenSecurity | null>(null);
const [isLoadingSecurity, setIsLoadingSecurity] = useState(false);
const [securityError, setSecurityError] = useState<string | null>(null);

4) Add a helper inside the component (useCallback) called loadTokenSecurity(). It should:
- If chainName not solana OR !address: setTokenSecurity(null) return.
- If tokenSymbol === "SOL": setTokenSecurity({tokenProgram:"SPL", mintable:false, freezable:false}) return. (We will render “Native SOL” anyway.)
- If cached in securityCache: set state from cache and return.
- Otherwise:
  - set loading true, clear error
  - mintPk = new PublicKey(address)
  - info = await connection.getAccountInfo(mintPk)
  - if no info => store Unknown in cache and state
  - ownerStr = info.owner.toBase58()
  - is2022 = ownerStr === TOKEN_2022_PROGRAM_ID.toBase58()
  - isSpl = ownerStr === TOKEN_PROGRAM_ID.toBase58()
  - programId = is2022 ? TOKEN_2022_PROGRAM_ID : TOKEN_PROGRAM_ID
  - mint = unpackMint(mintPk, info, programId)
  - sec = { tokenProgram: is2022?"Token-2022": isSpl?"SPL":"Unknown", mintable: mint.mintAuthority !== null, freezable: mint.freezeAuthority !== null }
  - cache it, set state
- catch: set securityError, setTokenSecurity(null)
- finally: set loading false
Dependency list must include address, chainName, tokenSymbol, connection.

5) Triggering:
- Add useEffect: when activeTab becomes "about", call loadTokenSecurity().
- Add useEffect on [address] to clear tokenSecurity and securityError.

6) UI:
In renderAboutTab (currently around lines ~584-654), insert a new section card “Contract & Security” using existing styles:
- Title: “Contract & Security”
- Container: reuse styles.statsCard with background theme.backgroundDefault
- Show states:
  a) isLoadingSecurity => ActivityIndicator + “Fetching on-chain security…”
  b) securityError => “Unable to load security details.”
  c) tokenSymbol === "SOL" => single row “Asset type: Native SOL”
  d) tokenSecurity exists => rows:
     - Token program (neutral/accent)
     - Mintable with YES/NO pill (YES danger, NO success)
     - Freezable with YES/NO pill (YES danger, NO success)
     - Optional tiny hint text row (small, textSecondary): “Mintable/Freezable means issuer retains extra control.”
Use the same row separators as Stats (borderTopWidth 1, borderColor theme.border).

7) Verify no web crash:
- Don’t use Node-only Buffer APIs here.
- Only the above imports and connection.getAccountInfo + unpackMint.

Acceptance tests you must run quickly:
- Open SPL token About: shows program SPL + correct YES/NO.
- Open Token-2022 token About: shows Token-2022 + correct YES/NO.
- Open SOL About: shows Native SOL.
- Switch assets: no stale security info.
- No repeated fetch spam (cache works).

Deliver back:
- Confirm which “connection” instance you reused from this screen (do not create a new Connection).
- Confirm changes are only in AssetDetailScreen.tsx.