Update the inline token selection modal inside client/screens/SwapScreen.tsx (Modal around ~line 604). Do NOT refactor/extract a new component. Implement “paste mint” support so Pump.fun mints (and any SPL mint) show token details and are selectable even if not in Jupiter token list.

CURRENT
- showTokenModal controls visibility
- tokenModalType = "input" | "output"
- modal renders searchable list from solanaTokenList.ts
- When user pastes a mint (ex: JHY8U8KYpBGHVV74YMjJYHdYMPfb1g7dppump), nothing shows because it’s not in the list.

TARGET (PRODUCTION)
1) Detect if search input is a likely Solana mint (base58, length 32–44).
2) When it is a mint:
   - Show a pinned “Custom token” row at the top immediately (even if list empty).
   - Fetch metadata/decimals via a server endpoint and populate the row (name/symbol/decimals/logo if possible).
   - Allow selecting it if decimals are known.
3) Cache custom tokens (recent list) so it’s fast next time.
4) Keep existing list-search behavior for normal searches.

========================================================
A) SERVER: add token metadata endpoint (if not already present)
========================================================
Add to server (Express) a route:

GET /api/swap/solana/token/:mint

Response:
{
  mint: string,
  symbol: string,
  name: string,
  decimals: number,
  logoURI?: string,
  verified?: boolean,
  sources: string[]
}

Implementation:
- Validate base58 + length 32–44 else 400.
- Try resolve from cached Jupiter token list first (if you already load it): match by mint => return verified=true.
- If not found, resolve decimals on-chain using @solana/spl-token getMint(connection, new PublicKey(mint)). Must succeed or return 404.
- Optional: best-effort name/symbol/logo using any existing metadata source you already have (if none, return symbol="UNKNOWN", name="Unknown Token").
- Cache results in-memory for 10 minutes.

NOTE: Do NOT introduce a new heavy dependency if not needed. Decimals on-chain is required.

========================================================
B) CLIENT: SwapScreen.tsx inline modal changes
========================================================
Inside SwapScreen.tsx:

1) Add helpers near top:
- isLikelySolanaMint(text):
  - trim
  - length 32–44
  - base58 regex /^[1-9A-HJ-NP-Za-km-z]+$/
- shortMint(m) => first 4 + "..." + last 4

2) Add state:
- tokenSearch: string (if not already)
- customTokenLoading: boolean
- customTokenError: string | null
- customTokenResult: { mint, symbol, name, decimals, logoURI?, verified? } | null
- recentCustomTokens: array (load from AsyncStorage)

3) When tokenSearch changes:
- If NOT mint-mode: keep current list filtering behavior (debounce optional).
- If mint-mode:
  - Immediately set customTokenLoading=true, customTokenError=null
  - First check AsyncStorage cache (recentCustomTokens) for that mint; if found, set customTokenResult instantly and stop loading.
  - Otherwise fetch:
    GET `${SERVER_BASE_URL}/api/swap/solana/token/${mint}`
    - On success: set customTokenResult
      - Save/update this mint in AsyncStorage key "swap_custom_tokens" (cap 25, most recent first)
    - On failure:
      - set customTokenError with readable message
      - set customTokenResult=null
      - custom token row should still render but disabled.

4) Render changes in the Modal:
- Above the FlatList results, render:
  a) If tokenSearch is mint-mode:
     Render a pinned row:
       - left: token icon (logo if available else generic circle)
       - title: customTokenResult?.symbol ?? "UNKNOWN"
       - subtitle: customTokenResult?.name ?? "Unknown Token"
       - right: show decimals, and badge "Unverified" if verified===false
       - second subtitle line: shortMint(mint)
       - If loading: show spinner
       - If error or decimals missing: show red text and DISABLE selection
     On press (only if customTokenResult && decimals >= 0):
       call existing token selection handler:
         if tokenModalType==="input" setInputToken(customTokenResult)
         else setOutputToken(customTokenResult)
       close modal and clear search.

  b) If tokenSearch is empty (or not mint-mode):
     show “Recent” section (optional but preferred):
       - render recentCustomTokens list above main list

5) IMPORTANT: Swap quote must use decimals from selected token object.
- Ensure wherever you build quote request, you use:
  inputToken.decimals and outputToken.decimals
  and mint addresses from selected tokens (not list-only).

6) Safety:
- If custom mint fetch returns 404 (no mint account): show “Invalid mint” and disable selection.
- Don’t crash if logoURI missing.
- Must be TypeScript clean.

========================================================
C) QUICK TESTS
========================================================
- Open token modal, paste:
  JHY8U8KYpBGHVV74YMjJYHdYMPfb1g7dppump
Expected:
- “Custom token” row appears instantly
- Within ~1–2s shows at least decimals; symbol/name if available
- Selectable if decimals fetched
- Selected token persists and quote attempts routing

Deliver:
- List files changed
- Ensure npm run check:types passes
- No component extraction/refactor.